version: "3.9"
services:
  mblabs-prod:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mblabs-prod
    ports:
      - 4000:4000
    working_dir: /usr/src
    environment:
      NODE_ENV: production
      PORT: 4000
      DATABASE_URL: mongodb+srv://admin:5cB6LelZ1s1nxwCw@mb-labs-api-cluster.ek7z23k.mongodb.net/mb_labs-api?retryWrites=true&w=majority
      DB_NAME: mb_labs-api
    volumes:
      - /usr/local/data:/var/lib/mongodb/data

